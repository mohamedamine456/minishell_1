-list functions for pipes (new, last, addback, addfront, size, clear) ==> done
-check (addback for command_list)	==> done
-free all allocation	==> still have some leaks
-finish spliting commands by the end of the week ==> have to work on redirections

-don't forget to see the is_option function ==> TODO

-check debugger problem of allocation memory for name in the command struct (prob below) when there is a ; at the end check it as soon as possible:
				["error: Couldn't apply expression side effects : Couldn't dematerialize a result variable: couldn't read its memory"] ==> Fixed (done)

- check leaks on pipe ==> still have some leaks
- 06/03/2021
	- tomorrow work on directions and finish some unfinished work and functions
	- check with multiple example to see if there is a segfault or nor
	- an empty command ( should fix that)



- after spliting with ';' and spliting with '|' each part would be split by '>' , '<' or '>>' from put_elements_command function
