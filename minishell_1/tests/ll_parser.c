# include <stdio.h>
# include <unistd.h>
# include <stdlib.h>

enum 

int main()
{
	int		i;
	char	*buffer;

	buffer = strcpy(buffer, "(hello, world)");
	while(buffer[i] != '\0')
	{

		i++;
	}
	return (0);
}
